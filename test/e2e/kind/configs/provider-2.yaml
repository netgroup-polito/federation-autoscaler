# Provider cluster #2 — second donor; same shape as provider-1. Two
# providers exercises the broker's selection / chunking math across more
# than one source.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  podSubnet: "10.244.0.0/16"
  serviceSubnet: "10.99.0.0/16"
nodes:
- role: control-plane
- role: worker
